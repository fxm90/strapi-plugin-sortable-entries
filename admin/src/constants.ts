/**
 * Represents the status of an asynchronous operation.
 *
 * @see AsyncState
 */
export enum AsyncStatus {
  Initial = 'initial',
  InProgress = 'in-progress',
  Success = 'success',
  Failed = 'failed',
}
